language: go

go:
  - 1.14

services:
  - docker

before_install:
  - docker build -t ess-openapi-servicebroker .
  - docker run -d -p 8000:8000 ess-openapi-servicebroker
  - docker ps -a

script:
  - go test -v